# ============================================
# HackAbhigna Hub - Server Environment Variables
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
ATLAS_URI=mongodb+srv://username:password@cluster.mongodb.net/hackabhigna?retryWrites=true&w=majority

# ============================================
# Cloudinary Configuration (File Storage)
# ============================================
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# Email Configuration (SendGrid)
# ============================================
SENDGRID_API_KEY=SG.your_sendgrid_api_key

# ============================================
# GitHub Configuration
# ============================================
GITHUB_TOKEN=ghp_your_github_personal_access_token
GITHUB_OWNER=hackabhigna2025-hub

# ============================================
# Certificate Generation Options
# ============================================

# Option 1: Force PDFKit (Recommended - No Installation Required)
# Set to 'true' to use PDFKit for certificate generation
# This works on all systems without needing LaTeX installed
FORCE_PDFKIT=true

# Option 2: Specify pdflatex path (if LaTeX is installed)
# Windows example: C:/Program Files/MiKTeX/miktex/bin/x64/pdflatex.exe
# macOS example: /Library/TeX/texbin/pdflatex
# Linux example: /usr/bin/pdflatex
# PDFLATEX_PATH=/path/to/pdflatex

# Option 3: Use PDF.co Cloud API (Production)
# Sign up at https://pdf.co to get an API key
# PDF_CO_API_KEY=your_pdf_co_api_key

# ============================================
# Optional: Nodemailer Configuration
# (Alternative to SendGrid)
# ============================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# ============================================
# Notes
# ============================================
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env file to version control
# 3. For certificate generation:
#    - FORCE_PDFKIT=true is the easiest option (recommended)
#    - Or install LaTeX and set PDFLATEX_PATH
#    - Or use PDF_CO_API_KEY for cloud-based generation
# 4. All Cloudinary and SendGrid keys are required for full functionality
# 5. GitHub token needs 'repo' scope for creating private repositories

